import React, { FC } from "react";
import MainLayout from "../../layouts/MainLayout";

const Create: FC = () => {
  return (
    <MainLayout>
      Create track
    </MainLayout>
  );
};

export default Create;

